name: orangecloud_im_demo
description: OrangeCloud IM SDK Flutter Demo
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  orangecloud_im_client:
    git:
      url: https://github.com/OrangeCloud-SDK-IM/orangecloud-im-flutter.git
      ref: v1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
